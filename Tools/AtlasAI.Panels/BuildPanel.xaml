<UserControl x:Class="AtlasAI.Panels.BuildPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Build Status" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="▶ Build" Margin="0,0,4,0" Click="Build_Click"/>
            <Button Content="⏹ Cancel" Margin="0,0,4,0" Click="Cancel_Click"/>
            <Button Content="🗑 Clear" Margin="0,0,0,0" Click="ClearBuild_Click"/>
            <TextBlock x:Name="BuildStatusLabel" Margin="16,2,0,0" Text="Idle" VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" x:Name="BuildScroller">
            <TextBlock x:Name="BuildOutputBlock" TextWrapping="Wrap"
                       FontFamily="Consolas" FontSize="11"
                       Text="Build output appears here."/>
        </ScrollViewer>

        <ProgressBar Grid.Row="3" x:Name="BuildProgress" Height="8"
                     Minimum="0" Maximum="100" Value="0" Margin="0,8,0,0"/>
    </Grid>
</UserControl>

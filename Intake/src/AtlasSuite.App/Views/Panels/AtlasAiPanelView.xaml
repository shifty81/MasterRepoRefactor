<UserControl x:Class="AtlasSuite.App.Views.Panels.AtlasAiPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="100" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="16" FontWeight="Bold" Text="AtlasAI" />
        <TextBox Grid.Row="1" Margin="0,8,0,8" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding Request, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="2" Width="100" HorizontalAlignment="Left" Content="Run" Command="{Binding DataContext.AskAtlasAiCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
        <TextBox Grid.Row="3" Margin="0,8,0,0" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding Response}" />
    </Grid>
</UserControl>
